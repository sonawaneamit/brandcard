import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Brandcard - Create On-Brand Images Instantly",
  description: "Generate beautiful, on-brand images from simple inputs. Perfect for social media, marketing, and content creation.",
  keywords: ["image generation", "brand assets", "social media", "marketing", "design"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}